export class Product{
    id: string;

    brand: string;
    model: string;

    cpu: string;  // detail
    gpu: string;  // detail
    ram: string;  // detail

    price: number;

    details: string;  // detail

    inStock: boolean;

    weight: number;
}
